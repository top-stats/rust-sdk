[package]
name = "example-async"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
topstats = { path = "../.." }
tokio = { version = "1.49", features = ["macros", "rt-multi-thread"] }
